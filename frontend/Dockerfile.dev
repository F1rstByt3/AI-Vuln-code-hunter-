# Dev image: pre-installs deps so `docker compose up` is fast on rebuilds.
# Source is volume-mounted so HMR works.
FROM node:20-alpine
WORKDIR /web
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
